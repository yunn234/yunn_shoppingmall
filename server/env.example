# 서버 포트
PORT=5001

# MongoDB 연결 URI
# MongoDB Atlas 사용 시 (우선 사용):
MONGODB_ATLAS_URL=mongodb+srv://username:password@cluster.mongodb.net/shopping-mall?retryWrites=true&w=majority

# 로컬 MongoDB 사용 시 (MONGODB_ATLAS_URL이 없을 때만 사용):
# MONGODB_URI=mongodb://localhost:27017/shopping-mall

# JWT Secret Key (프로덕션 환경에서는 반드시 강력한 랜덤 문자열로 변경하세요)
JWT_SECRET=your-secret-key-change-in-production

# 포트원(아임포트) API 키 (결제 검증용)
# 포트원 관리자 페이지에서 발급받은 REST API 키와 시크릿을 입력하세요
PORTONE_API_KEY=your-portone-api-key
PORTONE_API_SECRET=your-portone-api-secret

# CORS 허용 도메인 (프로덕션 환경)
# 여러 도메인을 허용하려면 쉼표로 구분하여 입력
# 예: ALLOWED_ORIGINS=https://your-app.vercel.app,https://your-custom-domain.com
ALLOWED_ORIGINS=https://your-vercel-app.vercel.app
